// declare a variable
let age = 23;

console.log(age);
 // output should be 23

const name = "Zyra";

 console.log(name);

// Scope = where you can reference a variable in your code.
  
    let favoriteColor = "blue";
    console.log(favoriteColor);   

document.querySelector('h1').style.color = favoriteColor;

// console.log(favoriteColor);  this will not work because favoriteColor is only available in the if statement scope.